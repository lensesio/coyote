- name: test
  entries:
    - name: ls
      command: |
        ls
        -lR
        .
    - name: echo
      command: echo hello
    - name: error
      command: ls /root


- name: more tests, oh yeah!
  entries:
    - name: base64
      command: |
        bash -c 'echo Hello World | base64'
